# Omarchy Walker Layout Search Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add bidirectional English/Russian physical-key layout search to Walker's `desktopapplications` provider by shipping a user-managed Elephant build and wiring Omarchy to use it.

**Architecture:** Keep the solution isolated under `omarchy-walker-layout-search/`, vendor a pinned Elephant source snapshot there, and patch only `internal/providers/desktopapplications`. Build a dedicated Elephant binary into `dist/bin/elephant`, point the user-level systemd service at that binary, and keep Omarchy-managed files untouched.

**Tech Stack:** Go 1.26.x, Elephant v2 upstream snapshot (`376ee71c66db38683daabd57350bf3f6f086eaf8`), systemd --user, Walker, Omarchy shell commands

---

## File Map

- Create: `omarchy-walker-layout-search/README.md`
- Create: `omarchy-walker-layout-search/.gitignore`
- Create: `omarchy-walker-layout-search/scripts/bootstrap-elephant.sh`
- Create: `omarchy-walker-layout-search/scripts/build-elephant.sh`
- Create: `omarchy-walker-layout-search/scripts/check-active-elephant.sh`
- Create: `omarchy-walker-layout-search/examples/desktopapplications.toml`
- Create: `omarchy-walker-layout-search/examples/elephant.service.d/override.conf`
- Create: `omarchy-walker-layout-search/vendor/elephant/` (generated by bootstrap script from upstream commit `376ee71c66db38683daabd57350bf3f6f086eaf8`)
- Create: `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/layout.go`
- Create: `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/layout_test.go`
- Create: `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/query_test.go`
- Modify: `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/query.go`
- Modify: `/home/miron/.config/elephant/desktopapplications.toml`
- Create: `/home/miron/.config/systemd/user/elephant.service.d/override.conf`

## Task 1: Scaffold The Solution Workspace

**Files:**
- Create: `omarchy-walker-layout-search/README.md`
- Create: `omarchy-walker-layout-search/.gitignore`
- Create: `omarchy-walker-layout-search/scripts/bootstrap-elephant.sh`

- [ ] **Step 1: Create the solution directory layout**

Run:

```bash
mkdir -p omarchy-walker-layout-search/scripts omarchy-walker-layout-search/examples/elephant.service.d omarchy-walker-layout-search/dist/bin omarchy-walker-layout-search/vendor
```

Expected: the directories exist and `git status --short` shows the new `omarchy-walker-layout-search/` paths.

- [ ] **Step 2: Add the bootstrap script that vendors Elephant at the pinned upstream commit**

Create `omarchy-walker-layout-search/scripts/bootstrap-elephant.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
VENDOR_DIR="$ROOT/vendor/elephant"
UPSTREAM_URL="https://github.com/abenz1267/elephant.git"
UPSTREAM_COMMIT="376ee71c66db38683daabd57350bf3f6f086eaf8"

rm -rf "$VENDOR_DIR"
git clone "$UPSTREAM_URL" "$VENDOR_DIR"
git -C "$VENDOR_DIR" checkout "$UPSTREAM_COMMIT"
rm -rf "$VENDOR_DIR/.git"
```

- [ ] **Step 3: Add repository docs and ignore rules for generated artifacts**

Create `omarchy-walker-layout-search/README.md`:

````md
# Omarchy Walker Layout Search

This solution vendors a pinned Elephant source tree, patches the `desktopapplications` provider for English/Russian physical-key layout matching, and builds a dedicated binary for Omarchy's Walker integration.

## Workflow

1. Run `./scripts/bootstrap-elephant.sh` to refresh `vendor/elephant/`.
2. Implement and test provider changes under `vendor/elephant/internal/providers/desktopapplications/`.
3. Run `./scripts/build-elephant.sh` to produce `dist/bin/elephant`.
4. Point the user-level `elephant.service` override at `dist/bin/elephant`.
```

Create `omarchy-walker-layout-search/.gitignore`:

```gitignore
dist/bin/elephant
vendor/elephant/cmd/elephant/elephant
vendor/elephant/.cache/
```

- [ ] **Step 4: Smoke-test the scaffold**

Run:

```bash
bash -n omarchy-walker-layout-search/scripts/bootstrap-elephant.sh && ./omarchy-walker-layout-search/scripts/bootstrap-elephant.sh && test -f omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/query.go
```

Expected: the script passes shell syntax validation, the vendored Elephant tree is created, and `query.go` exists at the expected path.

- [ ] **Step 5: Commit the scaffold**

Run:

```bash
git add omarchy-walker-layout-search/README.md omarchy-walker-layout-search/.gitignore omarchy-walker-layout-search/scripts/bootstrap-elephant.sh omarchy-walker-layout-search/vendor
git commit -m "chore: scaffold walker layout search workspace"
```

## Task 2: Add Layout Conversion Helpers With Failing Tests First

**Files:**
- Create: `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/layout_test.go`
- Create: `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/layout.go`
- Test: `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/layout_test.go`

- [ ] **Step 1: Write the failing helper tests**

Create `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/layout_test.go`:

```go
package main

import (
	"slices"
	"testing"
)

func TestBuildQueryVariantsAddsConvertedLayouts(t *testing.T) {
	variants := buildQueryVariants("firefox")
	queries := make([]string, 0, len(variants))

	for _, variant := range variants {
		queries = append(queries, variant.Query)
	}

	if !slices.Contains(queries, "firefox") {
		t.Fatalf("expected original query in variants, got %v", queries)
	}

	if !slices.Contains(queries, "ашкуащч") {
		t.Fatalf("expected converted russian query in variants, got %v", queries)
	}
	}

func TestBuildQueryVariantsDeduplicatesEquivalentQueries(t *testing.T) {
	variants := buildQueryVariants("123")
	if len(variants) != 1 {
		t.Fatalf("expected exactly one variant for non-letter input, got %d", len(variants))
	}
}

func TestConvertLayoutPreservesUnsupportedRunes(t *testing.T) {
	got := convertLayout("firefox-123", enToRuKeymap)
	if got != "ашкуащч-123" {
		t.Fatalf("expected converted query to preserve punctuation, got %q", got)
	}
}
```

- [ ] **Step 2: Run the tests to verify the RED state**

Run:

```bash
cd omarchy-walker-layout-search/vendor/elephant && go test ./internal/providers/desktopapplications -run 'TestBuildQueryVariants|TestConvertLayout' -count=1
```

Expected: FAIL with undefined identifiers such as `buildQueryVariants`, `convertLayout`, and `enToRuKeymap`.

- [ ] **Step 3: Add the minimal layout helper implementation**

Create `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/layout.go`:

```go
package main

import "strings"

const convertedMatchPenalty int32 = 15

type searchVariant struct {
	Query   string
	Penalty int32
}

var enToRuKeymap = map[rune]rune{
	'q': 'й', 'w': 'ц', 'e': 'у', 'r': 'к', 't': 'е', 'y': 'н', 'u': 'г', 'i': 'ш', 'o': 'щ', 'p': 'з',
	'[': 'х', ']': 'ъ', 'a': 'ф', 's': 'ы', 'd': 'в', 'f': 'а', 'g': 'п', 'h': 'р', 'j': 'о', 'k': 'л',
	'l': 'д', ';': 'ж', '\'': 'э', 'z': 'я', 'x': 'ч', 'c': 'с', 'v': 'м', 'b': 'и', 'n': 'т', 'm': 'ь',
	',': 'б', '.': 'ю', '/': '.',
}

var ruToEnKeymap = map[rune]rune{
	'й': 'q', 'ц': 'w', 'у': 'e', 'к': 'r', 'е': 't', 'н': 'y', 'г': 'u', 'ш': 'i', 'щ': 'o', 'з': 'p',
	'х': '[', 'ъ': ']', 'ф': 'a', 'ы': 's', 'в': 'd', 'а': 'f', 'п': 'g', 'р': 'h', 'о': 'j', 'л': 'k',
	'д': 'l', 'ж': ';', 'э': '\'', 'я': 'z', 'ч': 'x', 'с': 'c', 'м': 'v', 'и': 'b', 'т': 'n', 'ь': 'm',
	'б': ',', 'ю': '.', '.': '/',
}

func convertLayout(input string, keymap map[rune]rune) string {
	var builder strings.Builder
	builder.Grow(len(input))

	for _, r := range strings.ToLower(input) {
		if mapped, ok := keymap[r]; ok {
			builder.WriteRune(mapped)
			continue
		}

		builder.WriteRune(r)
	}

	return builder.String()
}

func buildQueryVariants(query string) []searchVariant {
	variants := []searchVariant{{Query: strings.ToLower(query), Penalty: 0}}
	seen := map[string]struct{}{strings.ToLower(query): {}}

	for _, candidate := range []string{
		convertLayout(query, enToRuKeymap),
		convertLayout(query, ruToEnKeymap),
	} {
		candidate = strings.ToLower(candidate)
		if _, ok := seen[candidate]; ok {
			continue
		}

		seen[candidate] = struct{}{}
		variants = append(variants, searchVariant{Query: candidate, Penalty: convertedMatchPenalty})
	}

	return variants
}
```

- [ ] **Step 4: Run the helper tests to verify GREEN**

Run:

```bash
cd omarchy-walker-layout-search/vendor/elephant && go test ./internal/providers/desktopapplications -run 'TestBuildQueryVariants|TestConvertLayout' -count=1
```

Expected: PASS.

- [ ] **Step 5: Commit the helper implementation**

Run:

```bash
git add omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/layout.go omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/layout_test.go
git commit -m "feat: add layout-aware query variants"
```

## Task 3: Patch Provider Scoring And Keep Direct Matches On Top

**Files:**
- Create: `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/query_test.go`
- Modify: `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/query.go`
- Test: `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/query_test.go`

- [ ] **Step 1: Write failing provider scoring tests**

Create `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/query_test.go`:

```go
package main

import "testing"

func testConfig() *Config {
	return &Config{OnlySearchTitle: false}
}

func TestCalcScoreMatchesEnglishNameFromRussianLayout(t *testing.T) {
	config = testConfig()
	data := &Data{Name: "firefox", GenericName: "browser", Comment: "web browser"}

	match, score, _, _, ok := calcScore("ашкуащч", data, false)
	if !ok {
		t.Fatal("expected converted russian layout query to match firefox")
	}

	if match != "firefox" {
		t.Fatalf("expected firefox match, got %q", match)
	}

	if score <= 0 {
		t.Fatalf("expected positive score, got %d", score)
	}
}

func TestCalcScoreMatchesRussianNameFromEnglishLayout(t *testing.T) {
	config = testConfig()
	data := &Data{Name: "терминал", GenericName: "terminal"}

	match, score, _, _, ok := calcScore("nthvbyfk", data, false)
	if !ok {
		t.Fatal("expected converted english layout query to match russian name")
	}

	if match != "терминал" {
		t.Fatalf("expected терминал match, got %q", match)
	}

	if score <= 0 {
		t.Fatalf("expected positive score, got %d", score)
	}
}

func TestCalcScorePrefersDirectMatchOverConvertedMatch(t *testing.T) {
	config = testConfig()
	data := &Data{Name: "firefox"}

	_, directScore, _, _, directOK := calcScore("firefox", data, false)
	_, convertedScore, _, _, convertedOK := calcScore("ашкуащч", data, false)

	if !directOK || !convertedOK {
		t.Fatalf("expected both direct and converted queries to match: direct=%t converted=%t", directOK, convertedOK)
	}

	if directScore <= convertedScore {
		t.Fatalf("expected direct score %d to exceed converted score %d", directScore, convertedScore)
	}
}

func TestCalcScoreStillSearchesNonTitleFields(t *testing.T) {
	config = testConfig()
	data := &Data{Name: "org.example.browser", GenericName: "браузер", Comment: "просмотр веб-страниц"}

	match, _, _, _, ok := calcScore("браузер", data, false)
	if !ok {
		t.Fatal("expected generic name to remain searchable")
	}

	if match != "браузер" {
		t.Fatalf("expected generic name match, got %q", match)
	}
}
```

- [ ] **Step 2: Run the scoring tests to verify the RED state**

Run:

```bash
cd omarchy-walker-layout-search/vendor/elephant && go test ./internal/providers/desktopapplications -run 'TestCalcScore' -count=1
```

Expected: FAIL because `calcScore` only checks the original query and does not yet apply layout variants or converted-match penalties.

- [ ] **Step 3: Patch `calcScore` to evaluate direct and converted variants**

Update `omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/query.go`:

```go
func calcScore(q string, d *Data, exact bool) (string, int32, []int32, int32, bool) {
	var scoreRes int32
	var posRes []int32
	var startRes int32
	var match string

	toSearch := []string{d.Name}
	if !config.OnlySearchTitle {
		toSearch = []string{d.Name, d.Exec, d.Parent, d.GenericName, strings.Join(d.Keywords, ","), d.Comment}
	}

	for _, variant := range buildQueryVariants(q) {
		for k, v := range toSearch {
			score, pos, start := common.FuzzyScore(variant.Query, v, exact)
			if score == 0 {
				continue
			}

			modifier := int32(k)
			score = max(score-min(modifier*5, 50)-start-variant.Penalty, 10)

			if score > scoreRes {
				scoreRes = score
				posRes = pos
				startRes = start
				match = v
			}
		}
	}

	if scoreRes == 0 {
		return "", 0, nil, 0, false
	}

	return match, scoreRes, posRes, startRes, true
}
```

- [ ] **Step 4: Run the full provider test set to verify GREEN**

Run:

```bash
cd omarchy-walker-layout-search/vendor/elephant && go test ./internal/providers/desktopapplications -count=1
```

Expected: PASS, including direct-match ranking, English<->Russian physical-key conversion, and non-title field search coverage.

- [ ] **Step 5: Commit the provider patch**

Run:

```bash
git add omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/query.go omarchy-walker-layout-search/vendor/elephant/internal/providers/desktopapplications/query_test.go
git commit -m "feat: score layout-converted desktop app queries"
```

## Task 4: Build, Install, And Wire The Custom Elephant Binary

**Files:**
- Create: `omarchy-walker-layout-search/scripts/build-elephant.sh`
- Create: `omarchy-walker-layout-search/scripts/check-active-elephant.sh`
- Create: `omarchy-walker-layout-search/examples/desktopapplications.toml`
- Create: `omarchy-walker-layout-search/examples/elephant.service.d/override.conf`
- Modify: `/home/miron/.config/elephant/desktopapplications.toml`
- Create: `/home/miron/.config/systemd/user/elephant.service.d/override.conf`

- [ ] **Step 1: Add the build and diagnostics scripts plus reusable config examples**

Create `omarchy-walker-layout-search/scripts/build-elephant.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
SOURCE_DIR="$ROOT/vendor/elephant"
OUTPUT_DIR="$ROOT/dist/bin"

mkdir -p "$OUTPUT_DIR"
make -C "$SOURCE_DIR" clean build
install -Dm755 "$SOURCE_DIR/cmd/elephant/elephant" "$OUTPUT_DIR/elephant"
```

Create `omarchy-walker-layout-search/scripts/check-active-elephant.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail

mapfile -t pids < <(pgrep -u "$USER" -x elephant)
pid="${pids[0]:-}"

if [[ -z "$pid" ]]; then
	printf 'elephant is not running\n' >&2
	exit 1
fi

readlink -f "/proc/$pid/exe"
systemctl --user show elephant.service --property=ExecStart
```

Create `omarchy-walker-layout-search/examples/desktopapplications.toml`:

```toml
show_actions = false
only_search_title = false
history = false
```

Create `omarchy-walker-layout-search/examples/elephant.service.d/override.conf`:

```ini
[Service]
ExecStart=
ExecStart=/home/miron/Omarchy-Stuff/omarchy-walker-layout-search/dist/bin/elephant
```

- [ ] **Step 2: Verify the scripts build and inspect the custom binary**

Run:

```bash
bash -n omarchy-walker-layout-search/scripts/build-elephant.sh omarchy-walker-layout-search/scripts/check-active-elephant.sh && ./omarchy-walker-layout-search/scripts/build-elephant.sh && file omarchy-walker-layout-search/dist/bin/elephant
```

Expected: both scripts pass syntax checks, `dist/bin/elephant` is built, and `file` reports an executable ELF binary.

- [ ] **Step 3: Update the active Omarchy configs to use the custom Elephant build**

Replace `/home/miron/.config/elephant/desktopapplications.toml` with:

```toml
show_actions = false
only_search_title = false
history = false
```

Create `/home/miron/.config/systemd/user/elephant.service.d/override.conf`:

```ini
[Service]
ExecStart=
ExecStart=/home/miron/Omarchy-Stuff/omarchy-walker-layout-search/dist/bin/elephant
```

- [ ] **Step 4: Reload and restart the user services, then verify the running binary**

Run:

```bash
systemctl --user daemon-reload && systemctl --user restart elephant.service && omarchy-restart-walker && ./omarchy-walker-layout-search/scripts/check-active-elephant.sh
```

Expected: the service restarts cleanly, Walker reconnects, and `check-active-elephant.sh` prints `/home/miron/Omarchy-Stuff/omarchy-walker-layout-search/dist/bin/elephant`.

- [ ] **Step 5: Commit the automation and tracked examples**

Run:

```bash
git add omarchy-walker-layout-search/scripts/build-elephant.sh omarchy-walker-layout-search/scripts/check-active-elephant.sh omarchy-walker-layout-search/examples/desktopapplications.toml omarchy-walker-layout-search/examples/elephant.service.d/override.conf
git commit -m "chore: add custom elephant build and service wiring"
```

## Task 5: Document Manual Verification And Reuse Instructions

**Files:**
- Modify: `omarchy-walker-layout-search/README.md`

- [ ] **Step 1: Extend the solution README with local verification commands and expected behavior**

Update `omarchy-walker-layout-search/README.md`:

```md
# Omarchy Walker Layout Search

This solution vendors a pinned Elephant source tree, patches the `desktopapplications` provider for English/Russian physical-key layout matching, and builds a dedicated binary for Omarchy's Walker integration.

## Build

```bash
./scripts/bootstrap-elephant.sh
./scripts/build-elephant.sh
cd vendor/elephant && go test ./internal/providers/desktopapplications -count=1
```

## Local Omarchy Integration

1. Copy `examples/desktopapplications.toml` to `~/.config/elephant/desktopapplications.toml`.
2. Copy `examples/elephant.service.d/override.conf` to `~/.config/systemd/user/elephant.service.d/override.conf`.
3. Run `systemctl --user daemon-reload && systemctl --user restart elephant.service`.
4. Run `omarchy-restart-walker`.
5. Confirm the running binary with `./scripts/check-active-elephant.sh`.

## Manual Verification Matrix

- `firefox` -> Firefox appears near the top.
- `ашкуащч` -> Firefox appears even with Russian layout active.
- `терминал` -> the Russian-named terminal entry appears directly.
- `nthvbyfk` -> the same Russian-named terminal entry still appears.
````

- [ ] **Step 2: Run the documented verification commands exactly as written**

Run:

```bash
./omarchy-walker-layout-search/scripts/bootstrap-elephant.sh && ./omarchy-walker-layout-search/scripts/build-elephant.sh && (cd omarchy-walker-layout-search/vendor/elephant && go test ./internal/providers/desktopapplications -count=1)
```

Expected: PASS, which proves the README instructions are executable end-to-end.

- [ ] **Step 3: Manually verify Walker behavior in the live Omarchy session**

Manual checklist:

```text
1. Open Walker normally.
2. Search for firefox.
3. Search for ашкуащч.
4. Search for терминал.
5. Search for nthvbyfk.
6. Confirm direct matches rank above converted matches when both are possible.
```

Expected: all four searches return the expected applications and the direct-language query ranks above the converted-layout query.

- [ ] **Step 4: Record any ranking issues before closing the feature**

If the converted-match penalty needs tuning, create a follow-up beads issue instead of expanding scope in-place:

```bash
bd create --title="Tune Elephant layout-match ranking" --description="Adjust converted-query penalty after live Omarchy verification for omarchy-walker-layout-search." --type=task --priority=3
```

Expected: ranking follow-up work is captured explicitly instead of being folded into this implementation without fresh tests.

- [ ] **Step 5: Commit the final docs update**

Run:

```bash
git add omarchy-walker-layout-search/README.md
git commit -m "docs: add walker layout search verification guide"
```

## Self-Review

- The plan covers repository scaffolding, helper-level TDD, provider-level TDD, build/service integration, and manual Omarchy verification from the approved spec.
- No placeholders remain; every file path, command, and config target is explicit.
- The plan keeps the patch isolated to `internal/providers/desktopapplications` and avoids editing `~/.local/share/omarchy/`.
